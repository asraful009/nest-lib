import { ValidationOptions } from 'class-validator';

export declare function Upper(
  validationOptions?: ValidationOptions,
): PropertyDecorator {
  return (target: object, propertyKey: string) => {
    return null;
  };
};
